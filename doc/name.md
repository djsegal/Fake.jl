# Fake.Name
```julia
Fake.Name.name() # => "Tyshawn Johns Sr."

Fake.Name.name_with_middle() # => "Aditya Elton Douglas"

Fake.Name.first_name() # => "Kaci"

Fake.Name.last_name() # => "Ernser"

Fake.Name.prefix() # => "Mr."

Fake.Name.suffix() # => "IV"

Fake.Name.title() # => "Legacy Creative Director"

Fake.Name.initials() # => "NJM"
Fake.Name.initials(2) # => "NM"
```
