# Fake.Demographic
```julia
Fake.Demographic.race() # => "Native Hawaiian or Other Pacific Islander"

Fake.Demographic.educational_attainment() # => "GED or alternative credential"

Fake.Demographic.demonym() # => "Panamanian"

Fake.Demographic.marital_status() # => "Widowed"

Fake.Demographic.sex() # => "Female"

Fake.Demographic.height() # => "1.61"

Fake.Demographic.height(:imperial) # => "6 ft, 2 in"
```
